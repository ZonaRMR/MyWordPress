package cn.suiseiseki.www.suiseiseeker.tools;

import android.os.AsyncTask;

import java.net.URI;

/**
 * Created by Suiseiseki/shuikeyi on 2016/5/16.
 */
public class LoadCategoriesTask extends AsyncTask<URI,Integer,Long> {

    protected Long doInBackground(URI... uris)
    {
        int count = uris.length;
        long totalSize = 0;
        return totalSize;
    }
}
